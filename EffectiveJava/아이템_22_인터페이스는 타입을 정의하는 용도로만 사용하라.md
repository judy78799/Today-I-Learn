## 아이템_22_인터페이스는 타입을 정의하는 용도로만 사용하라.

## 🔷 인터페이스의 용도

> 클래스가 어떤 인터페이스를 구현한다는 것은 자신의 인스턴스로 무엇을 할 수 있는지(행동)를 클라이언트에 얘기해주는 것.

### ✔ 인터페이스의 본질

- 인터페이스 = *“타입(규약)을 정의하는 도구”*
- 구현체가 어떤 행동을 제공하는지 클라이언트에게 알려줌
- 즉, 인터페이스는 “이 객체는 이런 행동을 할 수 있다”라는 계약(Contract)을 표현해야 한다.

### ✔ 상수 인터페이스(Constant Interface)란?

- 메서드 없이 public static final 필드만 잔뜩 들어 있는 인터페이스
- 단순히 값 묶음일 뿐인데 “타입처럼” 사용하게 됨 → **설계 목적 위배**

### **✔ 상수 인터페이스의 문제점**

| **문제** | **설명** |
| --- | --- |
| **캡슐화 파괴** | 내부 구현(상수)을 외부 API에 그대로 노출 |
| **타입 오염(Type Pollution)** | 구현 클래스의 네임스페이스가 쓰지도 않을 상수로 더러워짐 |
| **API 변경 불가** | 한번 인터페이스로 공개하면 제거 불가 → 바이너리 호환성 깨짐 |
| **의미 없는 타입 계층 구성** | “행동”이 없는 인터페이스를 구현하는 것은 논리적으로 잘못됨 |
➡ 즉, 인터페이스 본질을 훼손하고 API 구조 전체를 오염시킴

## 🚨 인터페이스를 잘못 사용한 예

### 🔶 상수 인터페이스 안티패턴 (캡슐화 파괴)

- 클래스 내부에서 사용하는 상수는 외부 인터페이스가 아닌 내부 구현에 해당됨. 즉 내부 구현을 클래스 API로 노출하는 행위.
### **내부 구현을 클래스 API로 노출하는 행위**
- 즉 **내부 구현 정보를 인터페이스를 통해 바깥으로 새게 된 것(캡슐화 완전 붕괴).**
- 내부 구현에 있는 상수 그룹이 외부 코드에서 직접 의존하게 됨.
- 바이너리 호환성을 위해 여전히 상수 인터페이스를 구현하고 있어야 함.
- final이 아닌 클래스가 상수 인터페이스를 구현한다면 모든 하위 클래스의 이름공간이 그 인터페이스가 정의한 상수들로 오염되어 버린다.
- java.io.ObjectStreamConstants 자바 플랫폼 라이브러리 → 인터페이스 잘못 활용한 예이니 따라해서는 안됨.

## ✅ 요약: 상수 인터페이스가 위험한 이유

| **문제** | **설명** |
| --- | --- |
| **캡슐화 파괴** | 클래스 내부 구현인 상수를 외부 API로 노출하게 됨 |
| **사용자가 의미 없이 상수에 접근** | 그 인터페이스를 구현했다는 이유로 public API처럼 보임 |
| **클라이언트가 내부 구현에 종속됨** | 상수를 없애거나 인터페이스 구조를 바꾸면 기존 바이트코드가 깨짐 |
| **바이너리 호환성 유지 부담** | 이후 릴리스에서 상수를 제거해도, 호환성 때문에 인터페이스를 계속 유지해야 함 |

- API 범위
    
**API의 범위 = 외부에서 “보이고”, “사용할 수 있는” 모든 것.**
    
    ---
    
    ## ✅ API의 범위란 무엇인가?
    
    - *API(Application Programming Interface)**의 범위는
    
    “외부 코드(클라이언트)가 접근하고 사용할 수 있는 모든 요소들의 집합”을 의미한다.
    
    즉, **외부 개발자가 사용하게 될 인터페이스 전체**라고 생각하면 된다.
    
    ---
    
    ## ✅ 구체적으로 API의 범위에 포함되는 것
    
    ### **✔️ 1. public 클래스**
    
    - 외부에서 new 로 생성하거나 접근 가능한 클래스
    
    ### **✔️ 2. public 메서드**
    
    - 외부에서 호출 가능한 메서드
    
    ### **✔️ 3. public 필드(가능하면 쓰지 말아야 함)**
    
    - 외부에서 접근 가능한 필드
        
        (보통 설계상 좋지 않기 때문에 getter/setter 사용)
        
    
    ### **✔️ 4. protected 메서드/필드 (상속 가능성이 있으면 포함될 수 있음)**
    
    - 외부 패키지에서 상속받아 사용하는 경우 API에 포함됨
    
    ### **✔️ 5. public 인터페이스**
    
    - 다른 구현체가 구현할 수 있는 계약(Contract)
    - 외부에 공개한 규칙
    
    ---
    
    ## ❌ API 범위에 포함되지 않는 것
    
    - private 메서드/필드
    - package-private 클래스
    - 내부 구현에서만 사용하는 헬퍼 클래스
    - 내부 구현용 상수
    - 내부 캐싱, 자료구조, 최적화용 코드
    
    이들은 **API가 아니라 내부 구현(detail)**.
    
    ---
    
    ## 📌 왜 API 범위가 중요하냐?
    
    API는 한 번 공개하면 **쉽게 변경할 수 없기 때문**이다.
    
    - 외부에서 호출하는 public 메서드를 마음대로 삭제하거나 시그니처 변경하면
        
        ➜ **기존 코드가 전부 깨짐**
        
    - 인터페이스 변경하면
        
        ➜ **모든 구현체 수정 필요**
        
    
    즉, API의 범위 = **함부로 변경하면 안 되는 영역**
    

## 🔷 상수 인터페이스 대안점

🤔 상수를 공개할 목적이라면?

- 특정 클래스나 인터페이스와 강하게 연관된 상수라면 → 그 클래스나 인터페이스 자체에 추가
- 열거 타입으로 나타내기 적합한 상수라면 → 열거 타입으로 만들어 공개하기
- else 인스턴스화 할 수 없는 유틸리티 클래스에 담아 공개하기.

상수 유틸리티 클래스 (PlysicalConstants의 유틸리티 클래스 버전)

```java
public class PhysicalConstants {
	private PhysicalConstants() {} //인스턴스화 방지
	
	//아보가드로 수 (1/몰)
	public static final double AVOGADROS_NUMBER = 6.022_140_857e23;
	//볼츠만 상수 (J/K)
	public static final double BOLTMANN_CONST = 1.380_648_52e-23;
	//전자 질량 (kg)
	public static final double ELECTRON_MASS = 9.109_383_56e-31;
}
//_를 사용하자 -> 위에서 사용한 숫자 리터럴(_)은 자바 7부터 허용
```

- 유틸리티 클래스에 정의된 상수를 클라이언트에서 사용하려면 클래스 이름까지 명시해야 한다. ex. PhysicalConstants.AVOGADROS_NUMBER
- 유틸리티 클래스의 상수를 빈번히 사용한다면 정적 임포트 하여 클래스 이름은 생략할 수 있다.

정적 임포트를 사용해 상수 이름만으로 사용하기

```java
import static effectivejava.chapter4.item22.constantutilityclass.PhysicalConstants.*;

public class Test {
	double atoms(double mols){
		return AVOGADROS_NUMBER*mols;
	}
	...
	//PhysicalConstants를 빈번히 사용한다면 정적 임포트가 값어치를 한다.
}
```

## ✅ 핵심 정리
인터페이스는 타입을 정의하는 용도로만 사용해야 한다. 상수 공개용 수단으로 사용하지 말자.

---

✚ public을 설계 관점에서 바라보자
    
대부분 개발 초반에는 public을 그냥 “외부에서도 쓰려고 열어둠” 정도로 생각

> **설계 관점에서는 public = 영원히 지켜야 하는 약속(API)** 라는 의미
⇒ public은 진짜 “가벼운 마음으로 쓰면 안 되는 가장 무거운 접근제어자” 임
> 

API 범위 : public, protected(상속과 관련 있는)

## ✅ 왜 public은 ‘가장 무거운 접근제어자’인가?

### **1) public으로 만드는 순간 → API가 된다**

- public은 외부에서 호출 가능
- = 외부 코드가 쓰기 시작하는 순간 “API가 됨”
- **== 한 번 공개하면 되돌릴 수 없는 약속**
- === API는 **절대로 쉽게 변경할 수 없음**
	
	→ 삭제, 시그니처 변경, 타입 변경하면 기존 코드 전부 깨짐
	

> 즉, public = **‘영원한 약속’**
함부로 열어준다는 건 “평생 책임질게요”라고 말하는 것과 같음.
> 

---

## ✅ 2) public으로 열어두면 내부 구현을 바꾸기 어려워진다

예를 들어,

```java
public static final int DEFAULT_SIZE = 20;
```

이게 단순한 내부 디테일이라고 생각하지만 public으로 노출하는 순간:

- 외부 모두가 이 DEFAULT_SIZE를 사용하게 됨
- 이 값을 변경하거나 제거하면 **바이너리 호환성 깨짐**
- 결과적으로 내부 구현을 바꿀 자유를 잃음

그래서 Effective Java는 말함:

> “public은 내부 구현이 아니라, 외부에 제공하는 기능에만 사용하라.”
> 

---

## ✅ 3) public은 API 표면적을 넓혀 사고 확률을 높인다

public이 많다는 것은:

- 내가 유지보수해야 할 API가 많다
- 실수할 확률이 올라간다
- 보안 취약점이 늘어난다
- 캡슐화가 무너진다

즉, public은 많을수록 독.

경험 많은 개발자일수록 public을 신중하게 쓰고

private → package-private → protected → public 순으로

가장 좁은 범위로 설계함.

---

## ✅ 4) 그래서 인터페이스도 public이면 끝장이다

인터페이스는 한 번 공개하면:

- 메서드 이름 하나, 파라미터 하나도 맘대로 변경 못함
- 모든 구현체가 영향을 받음
- API 변경 비용이 폭발적으로 커짐

그래서 인터페이스는 특히

**정말 잘 다듬어진 계약일 때만 public으로 내보내야 함.**
    
## 설계 경험이 쌓일수록

public을 줄이고,
private를 늘리고,
API 범위를 최소화하는 것이
버그를 줄이고 유지보수성을 크게 높인다는 것을 직접 느끼게 된다.
