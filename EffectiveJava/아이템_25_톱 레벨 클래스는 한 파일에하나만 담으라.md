# 아이템_25_톱 레벨 클래스는 한 파일에 하나만 담으라.md

## 톱 레벨 클래스란?
톱 레벨 클래스 = 파일 최상단에 정의된 클래스

```
class A { }   // ← 톱 레벨 클래스
```
자바에서 톱 레벨 클래스는 2가지 접근 제한자만 가능함.
- 👉 public (공개)
- 👉 (default) package-private (아무 것도 안 씀)

## 📌 톱 레벨 클래스 규칙 (중요)

1) 한 파일 안에 public 톱 레벨 클래스는 단 1개만 가능
```
public class A { }
public class B { } // ❌ 오류
```
파일 이름도 반드시 A.java여야 함.
2) public이 아닌 톱 레벨 클래스(default)는 여러 개 가능
```
class A { }
class B { } // 둘 다 package-private → 가능!
```

## 📦 톱 레벨 클래스 vs 중첩 클래스
- 중첩 클래스는 public/private/protected/default 다 가능, 
- 톱 레벨 클래스는 public 또는 default만 허용.

## ⚠️ 하나의 클래스 내부에 톱 레벨 클래스를 여러 개 정의하는 것
-> 🚨 심각한 위험을 감수해야 하는 행위
- 한 클래스를 여러가지로 정의할 수 있으며,
- 그 중 어느 것을 사용할 지는 어느 소스파일을 먼저 컴파일하냐에 따라 달라진다.

## ✅ 해결방안
- 서로 다른 소스 파일로 분리한다.
정적 멤버 클래스로 선언 -> 오직 하나의 클래스만 선언할 수 있음.
```
public class Test{
  public static void main(String[] args){
    System.out.println(Utensil.NAME + Dessert.NAME);
  }

  private static class Utensil{  //단 하나의 클래스로만 존재
    static final String NAME = "pan";
  }

  private static class Dessert{
    static final String NAME = "cake";
  }
}
```

